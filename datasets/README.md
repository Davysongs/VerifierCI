# Datasets

Curated datasets and manifests for verifier audit pilots are stored here.

- `manifests/` holds run manifests.
- `sample-panels/` stores small curated panel examples.
- `provenance/` tracks collection lineage and exposure history.
